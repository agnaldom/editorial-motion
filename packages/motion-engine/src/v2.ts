// SPEC V2 §32 — runtime v2 do motion-engine: módulos de avaliação por propriedade
// (evaluateTimeline/evaluateElement aqui; easing expandida §33). `resolveLayerState`
// (v1) continua exportado do pacote sem mudança — consumidores migram por issue.

export * from './v2/easing';
export * from './v2/evaluateTimeline';
export * from './v2/evaluateElement';
