{
  "name": "@editorial-motion/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/cache.test.ts"
  },
  "devDependencies": { "tsx": "^4.0.0", "typescript": "^5.0.0" }
}
