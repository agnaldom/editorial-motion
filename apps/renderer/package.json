{
  "name": "@editorial-motion/renderer",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "remotion studio src/index.ts",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts",
    "smoke": "node --import tsx --test src/smoke.spec.ts",
    "snapshot": "node --import tsx --test src/snapshot.spec.ts src/stagger.spec.ts src/parallax.spec.ts"
  },
  "dependencies": {
    "@editorial-motion/motion-engine": "workspace:*",
    "@remotion/cli": "^4.0.0",
    "@remotion/bundler": "^4.0.0",
    "@remotion/renderer": "^4.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remotion": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
