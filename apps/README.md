# Aplicações

O Milestone 0 reserva quatro aplicações: `web`, `api`, `renderer` e `vision-service`. Cada app terá seu próprio `package.json` ou `pyproject.toml` quando sua implementação começar, sempre em PR vinculado à Issue correspondente.
