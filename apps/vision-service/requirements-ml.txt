# Heavy ML providers for the vision service. Optional: base tests run without these.
# Install with: pip install -r requirements-ml.txt
torch>=2.4
transformers>=4.44
diffusers>=0.30
accelerate>=1.0
# simple-lama-inpainting fica de fora da resolução do pip: a versão 0.1.2 pinna
# pillow<10 (sem wheel para Python 3.12), o que forçaria downgrade do Pillow>=10
# do requirements.txt. É instalado com --no-deps no Dockerfile (vision-service.Dockerfile),
# junto com torchvision (dependência dele não coberta aqui).
# simple-lama-inpainting>=0.1.0
# Nome de distribuição é "sam-2" (o repositório renomeou); o pacote importado
# continua sendo `sam2`. pip>=25 rejeita `sam2 @ git+...` por inconsistência de nome.
sam-2 @ git+https://github.com/facebookresearch/sam2.git
opencv-python-headless>=4.10
